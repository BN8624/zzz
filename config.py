# =============================================================
#  config.py  ―  시스템 전체 설정 파일
# -------------------------------------------------------------
#  이 파일은 "설정의 뿌리"입니다.
#  다른 모든 파일(agents, orchestrator, database 등)이
#  여기 있는 값을 가져다 씁니다.
#  값을 바꾸고 싶으면 거의 항상 이 파일만 고치면 됩니다.
# =============================================================

import os
from dotenv import load_dotenv

# .env 파일(API 키 등 비밀값이 담긴 파일)을 읽어옵니다.
# .env 는 GitHub 에 올리지 않고, 서버에서 직접 만듭니다.
load_dotenv()


# -------------------------------------------------------------
#  1) API 키  ―  실제 키 값은 여기 적지 않습니다!
#     .env 파일에서 읽어옵니다. (코드 노출돼도 키는 안전)
# -------------------------------------------------------------
GOOGLE_API_KEY = os.getenv("GOOGLE_API_KEY")


# -------------------------------------------------------------
#  2) 사용할 모델 이름 (Google AI Studio 기준)
#     - 26B: 지시 / 디버깅 / 주제생성  담당
#     - 31B: 코딩 / 검토 / 평가        담당
#     두 모델이 역할을 3개씩 나눠 가져서 사용량이 균형을 이룹니다.
#
#     - flash_lite: 자가발전 루프 전용. "4점 미만 실패작 → 한 줄 교훈"
#       으로 압축하는 가벼운 요약 작업만 담당합니다(딱 한 가지 일).
#       3.1 Flash-Lite 무료 RPD 가 500 으로 작으니, 빈도 낮은 이 일에만 씁니다.
#     - (제거) 기존 "flash"(3.5 Flash, 배치 재처리용)는 폐지했습니다.
#       이유: 3점대 코드를 보완해 봐야 모델 능력 자체가 천장이라 효과가 없고,
#             3.5 Flash 는 유료라 무료 운영 원칙과도 맞지 않았습니다.
# -------------------------------------------------------------
MODELS = {
    "gemma_26b": "gemma-4-26b-a4b-it",       # 26B 모델 ID
    "gemma_31b": "gemma-4-31b-it",           # 31B 모델 ID
    "embedding": "gemini-embedding-2",       # 오답노트 의미검색용 임베딩 (임베딩2)
    #  ↑ 안 되면 test_key.py 로 정확한 모델 문자열을 한 번 확인하세요.
    "flash_lite": "gemini-3.1-flash-lite",   # 자가발전 교훈 정제용 (무료, RPD 500)
}


# -------------------------------------------------------------
#  3) 호출 속도 제한 (Rate Limit)
#     모델을 너무 빠르게 연속 호출하면 분당 한도(RPM)에 걸립니다.
#     그래서 호출과 호출 사이에 최소 몇 초를 강제로 쉽니다.
# -------------------------------------------------------------
RATE_LIMIT = {
    "min_interval_seconds": 6,   # 호출 사이 최소 6초 대기
}


# -------------------------------------------------------------
#  4) 하루 작업량 상한
#     RPD(하루 호출 한도)를 넘지 않도록 작업 수를 제한합니다.
#     질 우선 정책이라 무리하게 많이 돌리지 않습니다.
# -------------------------------------------------------------
DAILY_LIMIT = {
    "max_tasks_per_day": 200,    # 하루 최대 작업 개수
}


# -------------------------------------------------------------
#  5) 디버깅 반복 횟수
#     코딩 → AST → 디버깅 루프를 최대 몇 번 돌릴지.
#     숫자가 클수록 코드를 더 다듬습니다(질↑, 호출수↑).
#     "양보다 질" 정책이라 5로 둡니다.
# -------------------------------------------------------------
MAX_DEBUG_ITERATIONS = 5


# -------------------------------------------------------------
#  6) 결과 저장 정책
#     AI 평점이 일정 점수 이상일 때만 DB에 저장하고,
#     점수에 따라 보관 기간(며칠 뒤 자동 삭제)을 다르게 둡니다.
#     ⭐(즐겨찾기) 표시한 것은 영구 보관됩니다.
# -------------------------------------------------------------
DATA_RETENTION = {
    "min_score_to_save": 4.0,    # 4.0점 미만은 저장 안 함(즉시 폐기)
    #  ↑ (복귀) 3.0 → 4.0: 재처리 폐지로 3점대를 임시보관할 이유가 사라짐.
    #    4점 미만은 모델 한계로 보는 게 맞아, 미련 없이 즉시 폐기합니다.
    "auto_delete_days": {
        4.0: 7,    # 4.0점 → 7일 보관
        4.5: 14,   # 4.5점 → 14일 보관
        5.0: 30,   # 5.0점 → 30일 보관
    },
}


# -------------------------------------------------------------
#  6-1) 자가발전 루프 (오답노트 자동 기록) 설정
#     4점 미만(폐기되는) 작업에서 "한 줄 교훈"을 뽑아 오답노트에 남깁니다.
#     교훈 정제는 flash_lite 가 담당(가벼운 요약이라 적합 + RPD 절약).
#     오답노트가 무한정 커지면 검색이 느려지므로 상한을 둡니다.
#       - 상한 초과 시 usage_count(실제로 검색돼 쓰인 횟수)가 낮은 것부터 삭제.
#         (동점이면 오래된 것부터) → 쓸모없는 교훈이 자연 도태됩니다.
# -------------------------------------------------------------
SELF_IMPROVE = {
    "record_below_score": 4.0,   # 이 점수 미만이면 교훈을 기록
    "max_mistakes": 500,         # 오답노트 최대 보관 개수
}


# -------------------------------------------------------------
#  7) 호출량 페이싱 (유동 대기)  ★ 한도 제어의 핵심
#     RPD(하루 호출 한도)를 "넘은 뒤 막는" 대신, 처음부터 못 넘게
#     속도를 조절합니다. 작업이 끝날 때마다:
#         목표시간 = (그 작업이 쓴 호출 수) × seconds_per_call
#         대기     = max(0, 목표시간 − 그 작업에 실제로 걸린 시간)
#     이렇게 "호출 1회당 N초" 페이스를 유지하면, 하루 호출 수가
#     자연히 (86400 / N) 이하로 갇힙니다. 카운터가 리셋돼도,
#     반복 횟수가 들쭉날쭉해도 안전합니다.
#
#     seconds_per_call = 66  → 하루 약 1309회 (안전선; 26B/31B 각 1500 한도)
#       * 66초로 둔 이유: 1450~1500 에 딱 맞추지 않고 여유를 둬서,
#         콘솔 반영 지연이나 추정 오차를 흡수하기 위함.
# -------------------------------------------------------------
PACING = {
    "seconds_per_call": 66,   # 호출 1회당 확보할 시간(초). 클수록 느리고 안전.
}


# -------------------------------------------------------------
#  8) 파일 경로
# -------------------------------------------------------------
NOTEBOOK_PATH = "mistakes.json"   # 오답노트 저장 파일
DB_PATH = "code_results.db"       # 결과 DB 파일
LOG_PATH = "logs/agent.log"       # 로그 파일
